# Public URL of the deployed Express backend. Leave blank only for local Vite
# development, where /api requests are proxied to http://localhost:5000.
VITE_API_BASE_URL=

# Absolute public frontend URL, used for social sharing metadata at deploy time.
VITE_PUBLIC_SITE_URL=
